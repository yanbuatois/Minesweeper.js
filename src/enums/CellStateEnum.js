/**
 * @readonly
 * @enum {Number} Enumeration about the Cell's state.
 */
const CellStateEnum = {
  /** The cell is closed.*/
  CLOSED: 0,
  /** The cell is open. */
  OPEN: 1,
};

module.exports = CellStateEnum;
